# Reference material (manuals, SDKs, old vendor projects) - not part of this project's source
ref/

# .NET build output
[Bb]in/
[Oo]bj/
*.user

# Visual Studio
.vs/
*.suo
*.sdf

# Secrets / local runtime config - appsettings.local.json holds tokens/passwords for local (non-HA)
# runs; todo.txt/todo-note-store.json are runtime content/state. Normally only ever generated inside
# bin/, but ignore by name too as a safety net.
appsettings.local.json
todo.txt
todo-note-store.json
